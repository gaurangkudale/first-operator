# This rule is not used by the project first-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to demo.gaurangkudale.github.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: first-operator
    app.kubernetes.io/managed-by: kustomize
  name: myoperator-viewer-role
rules:
- apiGroups:
  - demo.gaurangkudale.github.io
  resources:
  - myoperators
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - demo.gaurangkudale.github.io
  resources:
  - myoperators/status
  verbs:
  - get
